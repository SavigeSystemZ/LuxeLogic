# MCP configs (templates)

This folder contains example MCP server config stubs that IdeaForge can generate per idea.

NOTE: MCP client config formats vary by host/IDE. Treat these as templates and adapt to your MCP client’s schema.

Security defaults
- Prefer read-only servers
- Keep filesystem access scoped and read-only by default
- Avoid mutation tools unless explicitly needed
