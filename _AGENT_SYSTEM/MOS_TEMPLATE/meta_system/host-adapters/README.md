# Host Adapters

These files are generated from `../meta-host-adapter-manifest.json`.

- First-class adapters represent supported startup contracts MOS actively validates.
- Spec-only adapters document the required authority chain for tools that do not yet have a stronger integration path in this repo.
