# Prompt Pack — M2: Idea capture UI + API

    ## Objective
    Deliver working UI + API for Idea CRUD, tags/status, and constraint editing.

    ## Canonical context (SSoT)
    Agents MUST treat these docs as truth:
    - system/design-docs/PRD.md
    - system/design-docs/ARCHITECTURE.md
    - system/design-docs/DATA_MODEL.md
    - system/design-docs/NFR.md
    - system/design-docs/RUNBOOK.if2.md

    ## Deliverables (this milestone)
    - UI screens for list/detail
- API endpoints for CRUD
- Input validation
- Tests + basic e2e

    ---

    ## PROMPT M2.0 — Planning (Cursor)
    ```text
    You are working in the repo for IdeaForge.
    Task: M2 (Idea capture UI + API) — PLANNING ONLY.

    Steps:
    1) Locate the relevant files/modules for this milestone.
    2) Propose a step-by-step plan with exact file paths you will touch.
    3) List risks and missing context as questions (max 3).
    4) WAIT for approval before implementing.

    Output sections: Plan, Files, Questions, Risks.
    ```

    ## PROMPT M2.1 — Implementation (Cursor)
    ```text
    Implement milestone M2 (Idea capture UI + API) following the approved plan.
    Constraints:
    - Stay strictly within milestone scope
    - Keep diffs minimal
    - No secrets in repo
    - Add/update tests as needed
    After edits:
    - Run validation per system/design-docs/RUNBOOK.if2.md
    - Summarize changes file-by-file
    ```

    ## PROMPT M2.1 — Implementation (Codex)
    ```text
    Task: M2 (Idea capture UI + API)
    Read: system/design-docs/PRD.md, system/design-docs/ARCHITECTURE.md, system/design-docs/DATA_MODEL.md, system/design-docs/NFR.md, system/design-docs/RUNBOOK.if2.md.
    Implement the smallest complete slice that satisfies acceptance criteria for this milestone.
    Add/update tests (including negative cases).
    Run validation commands from system/design-docs/RUNBOOK.if2.md and report results.
    Deliver:
    - Files changed
    - What changed (file-by-file)
    - How to verify locally
    - Follow-ups/risks
    ```

    ## PROMPT M2.2 — Tests (Codex)
    ```text
    Add missing tests for milestone M2.
    Focus on: edge cases, authz failures, input validation.
    Run tests and report results.
    ```

    ## PROMPT M2.1 — Structured (Gemini)
    ```text
    Role: Senior engineer + software architect.
    Task: Implement milestone M2 (Idea capture UI + API) for IdeaForge.

    Context (SSoT): system/design-docs/PRD.md / system/design-docs/ARCHITECTURE.md / system/design-docs/DATA_MODEL.md / system/design-docs/NFR.md / system/design-docs/RUNBOOK.if2.md

    Output required:
    1) Implementation plan (bullets)
    2) Checklist of deliverables
    3) Step-by-step instructions for an agent to make code changes (no code unless unavoidable)
    4) Validation steps
    ```

    ## PROMPT M2.4 — Review checklist
    ```text
    Review changes for milestone M2.
    Provide:
    - Bugs/edge cases
    - Security concerns
    - Maintainability issues
    - Missing tests/docs
    - Small refactors only (no rewrites)
    ```
