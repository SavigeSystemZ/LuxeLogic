# Plugins

Plugins extend MOS in controlled ways.

- place each plugin in its own directory
- include `plugin.json` and `README.md`
- keep owned paths explicit
- do not let plugins silently replace core contracts
