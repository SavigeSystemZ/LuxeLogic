# Load Master (full load order)

Run the full load order from **AGENTS.md** (items 1–18). Same as: *"load master architectural systems now!"*

**Do this now:**
1. Read and apply the load order in **AGENTS.md** (or **system/docs/AI_CONTEXT_INDEX.md** / **system/docs/CONTEXT_DOCUMENTS.md**). Load in sequence; if context is tight, load in step parts and confirm readiness after each part.
2. Log actions (what was loaded).
3. Confirm readiness to proceed under master prompt and rules.
4. If the full load risks context overflow, split into step parts and continue on request.

**Quick ref:** Key files — `system/prompts/MASTER_SYSTEM_PROMPT_ETHERWEAVE.md`, `system/docs/AI_CONTEXT_INDEX.md`, `system/docs/KERNEL_CONTRACT.md`, `PROJECT_RULES.md`, `MASTER_AI_REFERENCE.md`, `system/docs/ARCHITECTURE_BOUNDARIES.md`, `AGENTS.md`, `system/docs/CURSOR_NEXUS.md`, `docs/SESSION_STATE.md`.

Use the **etherweave-load-context** skill for the full step-by-step procedure.
