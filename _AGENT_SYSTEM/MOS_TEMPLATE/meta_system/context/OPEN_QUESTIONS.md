# Open Questions

- Which spec-only adapters should become first-class after real host validation exists?
- When MOS begins reviewing multiple downstream systems, should it gain a dedicated findings manifest format?
