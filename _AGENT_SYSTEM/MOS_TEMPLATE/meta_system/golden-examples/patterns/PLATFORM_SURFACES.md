# Platform Surfaces

Different downstream systems may target web, mobile, desktop, CLI, or API products, but the meta-system should stay neutral while still giving useful guidance.

## Pattern

- category guidance describes quality bars, not framework lock-in
- platform defaults should be explicit if the downstream system chooses them
- runtime platform decisions stay in the downstream system, not in the meta-layer
