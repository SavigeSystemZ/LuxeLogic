# Load system files (alias)

Load the **system prompt and core system files** in order. Shorter than full context load.

Do this now:
1. Read in order: `system/prompts/MASTER_SYSTEM_PROMPT_ETHERWEAVE.md`, `AGENTS.md` (load order), `system/docs/KERNEL_CONTRACT.md`, `PROJECT_RULES.md`, `system/docs/ENVIRONMENT_AND_MCP_MODEL.md`.
2. Be aware of `system/docs/CURSOR_NEXUS.md` (Skills, Commands, Subagents, Rules) and `.cursor/rules/` (always-apply rules).
3. State briefly: "System prompt and core files loaded; ready under master prompt and rules."

For full context (all 19 load-order items), use `/load-context` or `/ctx`.
