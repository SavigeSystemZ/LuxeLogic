# Skill: Templates + Recipes

Steps
1) Apply idea-type template to fill required fields and constraints.
2) Execute recipe steps; persist a run DAG.
3) Version artifacts; never overwrite.
4) Provide human review points (lock/approve).
5) Export with manifest + provenance and redaction.

Outputs
- Template artifacts
- Recipe artifacts
- Generated SSoT docs + prompt packs
