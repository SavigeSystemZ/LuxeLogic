interface:
  display_name: "Memory Ops"
  short_description: "Operate the local memory store and server"
  default_prompt: "Use $memory-ops to manage Candle Compass memory ingest and search."
dependencies:
  tools:
    - type: "mcp"
      value: "candle_compass-memory"
      description: "Local Candle Compass memory server"
      transport: "streamable_http"
      url: "http://127.0.0.1:8766/mcp"
