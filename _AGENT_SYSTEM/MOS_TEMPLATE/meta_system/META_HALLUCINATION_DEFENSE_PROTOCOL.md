# Meta Hallucination Defense Protocol

- Ground claims in repo-local files and validation output.
- Flag references to nonexistent `META_*` files, prompt packs, plugins, or adapter surfaces.
- Treat generated adapters and bundles as derived outputs, not the source of truth.
- Record confidence based on evidence, not intent.
