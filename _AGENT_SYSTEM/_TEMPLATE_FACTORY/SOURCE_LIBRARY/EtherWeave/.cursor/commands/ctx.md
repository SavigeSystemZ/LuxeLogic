# Load context (alias)

**Same as `/load-context`.** Load master system prompt, context, and rules in canonical order.

Do this now:
1. Read and apply the load order from `AGENTS.md` or `system/docs/AI_CONTEXT_INDEX.md`. If context is tight, load in step parts.
2. Key files: `system/prompts/MASTER_SYSTEM_PROMPT_ETHERWEAVE.md`, `system/docs/AI_CONTEXT_INDEX.md`, `system/docs/KERNEL_CONTRACT.md`, `PROJECT_RULES.md`, `MASTER_AI_REFERENCE.md`, `AGENTS.md`, `system/docs/ENVIRONMENT_AND_MCP_MODEL.md`.
3. After load, state briefly what was loaded and that you are ready.

Use the **etherweave-load-context** skill for the full step-by-step procedure.
