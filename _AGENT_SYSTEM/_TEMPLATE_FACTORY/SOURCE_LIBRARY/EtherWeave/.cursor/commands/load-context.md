# Load Master Context

Load the Etherweave master system prompt, prompts, and rules in canonical order.

**Do this now:**
1. Read and apply the load order from `AGENTS.md` or `docs/AI_CONTEXT_INDEX.md` (Part A → Part B → Part C). If context is tight, load in step parts and confirm readiness after each part.
2. **For Cursor**: After load order, be aware of MCP servers (docs/MCP_SETUP.md). Use Semgrep for security scans, Ref for doc search, Jam for debug context, Vale for doc linting when the task fits.
2. Key files: `prompts/MASTER_SYSTEM_PROMPT_ETHERWEAVE.md`, `docs/AI_CONTEXT_INDEX.md`, `docs/KERNEL_CONTRACT.md`, `PROJECT_RULES.md`, `MASTER_AI_REFERENCE.md`, `docs/ARCHITECTURE_BOUNDARIES.md`, `AGENTS.md`, `docs/SUBAGENT_PROTOCOL.md`, `docs/SESSION_STATE.md`.
3. After load, state briefly what was loaded and that you are ready to proceed under master prompt and rules.

Use the **etherweave-load-context** skill for the full step-by-step procedure.
