# Load Rules (nexus + rules only)

Load the Cursor nexus and rules index only — lighter than full context. Use when you need to confirm Skills, Commands, Subagents, and Rules without loading the full doc set.

**Do this now:**
1. Read **docs/CURSOR_NEXUS.md** (Skills, Commands, Subagents, Rules — when to use each).
2. Read **.cursor/rules/README.md** (rules index and always-apply list).
3. Optionally skim **.cursor/commands/README.md** and **.cursor/skills/README.md**.
4. State briefly that rules and nexus are loaded; use `/` for commands and **docs/CURSOR_NEXUS.md** for when to use skills/subagents.

For full context (master prompt + all docs), use **/load-master** or **/load-context** instead.
